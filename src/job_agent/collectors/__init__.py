"""Коллекторы сырья (стадия 1 — сбор).

Каждый источник — адаптер за общим интерфейсом `Collector.fetch(since)`.
Хрупкость вёрстки/сети изолирована в адаптере; оркестратор работает только
с `list[RawPost]`.
"""

from .base import Collector

__all__ = ["Collector"]
