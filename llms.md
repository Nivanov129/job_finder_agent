# llms.md — отдай этот файл нейросети, и она поможет установить Job Agent

> **Как пользоваться (для человека):** открой любого AI-ассистента (ChatGPT,
> Claude, и т.п.), вставь весь этот файл и напиши: «помоги установить по этой
> инструкции». Дальше просто следуй подсказкам и отвечай на вопросы ассистента.

---

## Инструкция для нейросети (читай внимательно)

Ты — **дружелюбный помощник по установке**. Перед тобой **не технический человек**:
он не программист, боится терминала и незнакомых слов. Твоя задача — довести его
до работающей программы спокойно и по шагам.

**Правила общения:**
- **Один шаг за раз.** Не вываливай всю инструкцию сразу. Дал шаг — дождись «готово».
- **Простой язык.** Никакого жаргона. Не «склонируй репозиторий», а «скачай папку
  с программой». Если без термина никак — объясни его в одном предложении.
- **Проси подтверждение и ошибки.** После каждого шага спрашивай результат. Если
  что-то пошло не так — попроси скопировать текст ошибки или прислать скриншот.
- **Подсказывай руками.** Где кнопка, что нажать, куда вставить. Учитывай, что
  человек может не знать, как открыть терминал или куда смотреть.
- **Успокаивай.** «Сборка идёт 5–15 минут — это нормально, просто подожди».
- **Сначала спроси систему:** Mac или Windows — команды разные.
- В конце убедись, что у человека **открылся сайт `http://localhost:8766`** и он
  понимает, что делать дальше (настройка в браузере).

---

## Что это за программа (объясни человеку своими словами)

**Job Agent** — личный помощник по поиску работы. Он сам собирает вакансии из
Telegram-каналов и сайтов, читает их искусственным интеллектом и сравнивает с
твоим резюме: показывает, насколько вакансия тебе подходит (**резюме %**) и
насколько она тебе интересна (**карта %**). Ещё готовит черновик сопроводительного
письма и помогает найти контакты, кому написать.

Главное: **всё работает на твоём компьютере**, данные никуда не уходят. Программа
бесплатная — нужен только твой доступ к ИИ (подписка на Claude или ChatGPT, либо
бесплатный локальный вариант).

---

## Что понадобится (проверь с человеком заранее)

- **Компьютер** Mac или Windows, ~30 минут времени и интернет.
- **Доступ к ИИ** — одно из:
  - **бесплатно, без подписки** — ключ **OpenRouter** или **Ollama Cloud**
    (берётся за минуту на сайте, банковская карта не нужна; вставляется на экране
    «AI · авторизация»). Самый простой старт;
  - подписка **Claude** (Claude Code) или **ChatGPT** (Codex);
  - или ключ API Anthropic/OpenAI;
  - или локальный **Ollama** на своём компьютере (бесплатно, но медленнее).
- **Telegram** — чтобы программа читала каналы с вакансиями, на которые человек подписан.

---

## Установка — веди по шагам

Хорошая новость: ставится **одной командой**. Она сама скачает программу,
**поставит Docker если нужно** («коробку», в которой всё работает), соберёт и
запустит. Качать и распаковывать вручную ничего не надо.

### Шаг 1. Открыть терминал

Терминал — окно, куда вставляют команды. Звучит страшно, но нужно всего один раз.

- **На Mac:** нажми `Cmd + Пробел`, набери **`Терминал`** (или `Terminal`), Enter.
- **На Windows:** нажми «Пуск», набери **`PowerShell`**, открой его.

### Шаг 2. Вставить одну команду

Скопируй строку под нужную систему, вставь в терминал (на Mac вставка — `Cmd+V`,
в PowerShell — правый клик) и нажми **Enter**:

**Mac / Linux:**
```
curl -fsSL https://raw.githubusercontent.com/Nivanov129/job_finder_agent/main/get.sh | sh
```

**Windows (PowerShell):**
```
irm https://raw.githubusercontent.com/Nivanov129/job_finder_agent/main/get.ps1 | iex
```

Что произойдёт: команда скачает программу в папку `job-agent`, **сама поставит
Docker, если его нет** (на Mac попросит **пароль от компьютера** — это нормально,
вводи; Docker при первом запуске может попросить **принять условия** — нажми
Accept), соберёт и запустит. **Первый раз 5–15 минут — это нормально, просто
ждём** (качается Docker и модель ИИ).

> **Если что-то не пошло:**
> - **Самое частое (Windows): ошибка про `docker_engine` / `npipe` / «демон
>   Docker не отвечает».** Это значит, что Docker Desktop установился, но **сам
>   движок ещё не запущен**. По шагам:
>   1) открой **«Docker Desktop»** из меню Пуск;
>   2) при первом запуске **прими условия** (Accept);
>   3) если попросит — **обнови WSL2 и перезагрузи компьютер** (Docker на Windows
>      работает через WSL2 — это нормальное требование);
>   4) дождись, пока **значок кита** покажет **«Docker Desktop running»** (зелёный);
>   5) **запусти ту же команду установки снова** — она докрутит до конца.
> - Windows ругается на политику запуска → пусть выполнит
>   `Set-ExecutionPolicy -Scope Process -ExecutionPolicy Bypass` (нажать `Y`) и
>   повторит команду.
> - Авто-установка Docker не сработала (редко) → поставь Docker вручную
>   https://www.docker.com/products/docker-desktop/ (Mac на Apple M1/M2/M3 —
>   «Apple Silicon», постарше — «Intel»; Windows — «Windows»), запусти Docker
>   Desktop, дождись спокойного значка кита и повтори команду.
> - Нет `curl`/`git` — обычно есть; если нет, поставить через «Командную строку
>   разработчика» или попросить помощи.
>
> **Повторный запуск команды установки безопасен и даже полезен:** он не стирает
> уже введённые настройки, сам подтягивает свежую версию программы и пересобирает.
> Поэтому «запусти ту же команду снова» — универсальное лекарство почти от всего.

### Шаг 3. Откроется сайт программы

Когда установка закончится, в браузере откроется **`http://localhost:8766`** —
это и есть Job Agent. Если браузер не открылся сам, пусть человек откроет ссылку
вручную (на первом запуске сайт может быть готов через ~1 минуту — обновить).

> На самом первом запуске сайт может быть недоступен ещё ~1 минуту — программа
> подгружает модель. Просто обновить страницу через минуту.

---

## Настройка в браузере (веди по экранам, слева — меню)

Скажи человеку: всё дальше — мышкой на сайте, терминал больше не нужен.

1. **Настройка** — загрузи **резюме** (PDF), при желании добавь **карту поиска**
   (примеры вакансий мечты) и **шаблон сопроводительного**. «Допустимые роли»
   подставятся из резюме сами. Можно добавить несколько направлений.
2. **AI · авторизация** — выбери движок:
   - проще всего **бесплатно**: **OpenRouter** или **Ollama Cloud** — возьми ключ
     на их сайте (кнопка-подсказка рядом, карта не нужна), вставь в поле и нажми
     «Проверить»;
   - или **Codex / Claude Code** по подписке — нажми войти, сайт сам выдаст
     ссылку/код для входа.
   В конце нажми «Проверить», чтобы убедиться, что связь есть.
3. **Telegram** — вход в свой Telegram, чтобы программа читала твои каналы:
   - **Сначала api_id и api_hash** (один раз, бесплатно, ~2 минуты): открой
     **my.telegram.org** → войди по своему номеру → «API development tools» →
     создай приложение (App title и short name — любые, например «job-agent») →
     скопируй **api_id** (число) и **api_hash** (строка) в поля на экране.
     Это идентификатор приложения Telegram; нужен, чтобы Telegram пускал
     программу читать каналы. Вводится один раз и запоминается.
   - **Потом номер телефона** → нажми «Получить код». Код придёт **сообщением
     в приложение Telegram** (от аккаунта «Telegram»), **не по SMS** — открой
     Telegram на телефоне, там код. Если включена 2FA — попросит пароль.
   - Программа покажет твои каналы и сама отметит те, что про вакансии.
4. **Подбор за период** — выбери период (например, неделя) и запусти. Появится
   живая воронка, а потом — **Подборка** с карточками вакансий.
5. **Подборка** — карточки с двумя процентами и ползунком «совпадение по карте».
   Кнопки: открыть вакансию, письмо, найти контакт.

---

## Если что-то пошло не так (частые ситуации)

- **«Docker не найден» / «демон не отвечает» / ошибка про `docker_engine` или
  `npipe` (Windows)** → Docker Desktop не запущен. Открыть «Docker Desktop» из
  Пуска, принять условия, при просьбе обновить WSL2 и **перезагрузиться**,
  дождаться значка кита «Docker Desktop running», затем запустить установщик
  снова.
- **Сборка идёт очень долго** → на первом запуске это нормально (качаются базовые
  слои и модель). Подождать. Прерывать не надо.
- **`permission denied` (Mac)** → `chmod +x install.sh`, затем `./install.sh`.
- **PowerShell не запускает скрипт (Windows)** →
  `Set-ExecutionPolicy -Scope Process -ExecutionPolicy Bypass`, потом `.\install.ps1`.
- **Сайт `localhost:8766` не открывается** → подожди минуту (идёт прогрев модели) и
  обнови страницу. Если совсем нет — проверь, что Docker запущен.
- **«Порт занят»** → у человека уже что-то висит на 8766; попроси прислать ошибку,
  объясни, что можно остановить старое: в терминале в папке программы
  `docker compose down`, затем заново.
- **Любая непонятная ошибка** → попроси скопировать **весь красный текст** и
  разбери его; почти всё лечится перезапуском Docker и повторным `./install.sh`.

Полезные команды (в терминале, в папке программы):
- Посмотреть, что происходит: `docker compose logs -f`
- Остановить программу: `docker compose down`
- Запустить снова: `docker compose up -d` (или снова установщик).

---

## Важно — скажи это человеку

- **Приватность.** Программа работает на его компьютере. Данные (резюме, переписка
  Telegram) никуда не уходят, кроме выбранного ИИ и поиска. Ни рекламы, ни слежки.
- **Никаких автооткликов.** Программа только находит и готовит — отправляет отклики
  и сообщения человек сам, руками.
- **Always-on.** Авто-поиск работает, только пока компьютер включён. Выключил —
  программа «спит»; пропущенное потом догонит «Подбор за период».
- **Секреты.** Пароли и токены хранятся локально и не передаются в этот чат — если
  ассистент вдруг просит прислать пароль/токен, этого делать не нужно.

Цель достигнута, когда у человека открыт `http://localhost:8766`, загружено резюме,
подключён ИИ и прошёл первый «Подбор за период» с карточками вакансий.

---

## Для ассистента: карта кода и отладка по репозиторию (продвинутое)

Этот раздел — **для тебя, ассистент**, не для человека. Если возникла ошибка,
которую не лечит «перезапусти команду / запусти Docker», — **разберись по
исходникам**, не угадывай. Проект открыт:

- **Репозиторий:** https://github.com/Nivanov129/job_finder_agent (ветка `main`).
- **Локально установлен в:** `~/job-agent` (Mac/Linux) или
  `%USERPROFILE%\job-agent` (Windows). Там же лежат исходники — можно открыть и
  читать файлы прямо у человека.
- **Сначала прочитай контекст:** `README.md` (обзор), `CLAUDE.md` (конвенции и
  **инварианты, которые нельзя нарушать**), `docs/FEATURES.md` (что реально
  работает), `docs/process-flow.md` (стадии пайплайна).
- **Загляни в историю:** `git -C ~/job-agent log --oneline -20` — частую ошибку
  могли уже починить; тогда решение = `git pull` (или просто **перезапустить
  установочную команду** — она сама делает `git reset --hard origin/main`).

### Где что лежит

| Область | Файлы |
| --- | --- |
| Установка / запуск | `install.sh`, `install.ps1` (Docker-check → `git pull` → `compose build` → `up`); `get.sh`, `get.ps1` (бутстрап: скачать/обновить репо и запустить install) |
| Docker-стек | `compose.yml` — сервисы: **`webui`** (порт 8766), **`pipeline`** (прогон), **`searxng`** (web-поиск), **`model-init`** (прогрев эмбеддингов); `Dockerfile` |
| Веб-интерфейс (FastAPI) | `webui/app.py` — все эндпоинты (`/engine*`, `/contacts*`, `/telegram*`, `/resume/roles`, `/save`); `webui/render.py` (HTML экранов); `webui/forms.py` (разбор форм) |
| Ключи/секреты | `webui/env_store.py` — чтение/запись `data/.env`; статусы движков — `webui/engine_status.py` |
| Telegram-вход | `webui/telegram_login.py`, `webui/login_flow.py` |
| AI-движки | `src/job_agent/engines/`: `cli.py` (Claude/Codex), `api_key.py` (Anthropic/OpenAI/**OpenRouter**), `ollama.py`; фабрика — `engines/__init__.py` (`make_engine`) |
| Пайплайн подбора | `src/job_agent/pipeline.py` (сшивка стадий); источники — `src/job_agent/collectors/` (`telegram_public/private`, `habr`, `getmatch`, `vseti`, `linkedin`) |
| Обработка вакансий | `normalize.py`, `titlefilter.py`, `prefilter.py`, `scoring.py`, `dedup.py`, `embeddings.py` (всё в `src/job_agent/`) |
| Контакты к вакансии | `src/job_agent/enrich/contacts.py` (+ investigator); web-поиск — `src/job_agent/websearch/` |
| Конфиг / цвета / промпты | `src/job_agent/config.py` + `config.schema.json`; `presentation.py` (единый источник цветов/иконок); `prompts/` (контракты — менять только по явной задаче) |
| Данные пользователя (НЕ в git) | `data/config.json`, `data/.env` (ключи, Telegram-сессия). **Никогда не показывай и не проси содержимое `data/.env`.** |

### Частые ошибки → где смотреть → решение

- **`npipe ... docker_engine` / «демон Docker не отвечает»** → Docker Desktop не
  запущен. Решение — раздел «Если что-то пошло не так» выше (запустить Docker
  Desktop, WSL2/ребут, перезапустить команду). Логика старта — в `install.*`.
- **PowerShell: «Missing closing `}`», «оператор `<` зарезервирован», крошево
  вместо кириллицы** → старый/битый `install.ps1` (был баг с кодировкой и с
  падением на `docker info` в PS7). Решение: **перезапустить установочную
  команду** — она подтянет исправленный скрипт.
- **На «AI · авторизация»: «движок недоступен … требует ключ»** → ключ не дошёл
  до движка. Проверь, что ключ вставлен и сохранён; затем пересобрать webui:
  `docker compose up -d --build webui`. Код: `webui/app.py` (подгрузка `.env` в
  окружение перед `make_engine`), `webui/env_store.py`, `engines/api_key.py`/`ollama.py`.
- **Контакты по ссылке `hh.ru` пустые/не те** → hh.ru отдаёт анти-бот/JS-страницу,
  простой загрузчик её не парсит. Решение: загрузить вакансию **PDF** или вписать
  **роль+компанию вручную**. Код: `webui/app.py` → `_fetch_url_text` и
  эндпоинт `/contacts/search`; `enrich/contacts.py`.
- **`localhost:8766` не открывается** → подожди прогрев модели; проверь
  `docker compose ps` и `docker compose logs -f webui`. Конфиг сервиса — `compose.yml`.
- **«Порт занят»** → `docker compose down`, затем заново. Порты — в `compose.yml`.

### Как действовать при ошибке

1. Получи **точный текст ошибки** (скриншот/копия) и какой шаг его вызвал.
2. Найди по таблице нужный файл, **прочитай его** в `~/job-agent` (или на GitHub) —
   не предлагай правку вслепую.
3. Сверься с `git log` и `CLAUDE.md`: возможно, уже исправлено (→ `git pull` /
   перезапуск команды) или правка нарушила бы инвариант (тогда не делай так).
4. Предложи **конкретное** решение с указанием файла (и строки, если правишь код).
   Технические правки в Python — после них прогнать `uv run ruff check src tests` и
   `uv run pytest -q` (требование `CLAUDE.md`).
5. Безопасность: не выводи секреты из `data/.env`; не предлагай отправлять
   токены/пароли в чат.
